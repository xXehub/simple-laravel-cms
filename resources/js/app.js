// Import Tabler functionality if needed
// Tabler already includes Bootstrap JS, so we don't need custom toggle logic

// Optional: Add any custom JavaScript here if needed
console.log("App JS berhasil di load - Tabler udah berfungsi dengan benar");
